# Telegram Bot - Информация о пользователе

Простой бот на aiogram, который показывает всю доступную информацию о пользователе после активации.

## 🚀 Установка

1. Убедитесь, что у вас установлен Python 3.8+

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Получите токен бота у [@BotFather](https://t.me/BotFather):
   - Откройте @BotFather в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

5. Добавьте токен в файл `.env`:
```
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

## 🏃 Запуск

```bash
cd bot
python bot.py
```

Или из корневой директории проекта:
```bash
python bot/bot.py
```

## 📋 Команды

- `/start` - Начать работу с ботом и получить информацию о себе
- `/info` - Показать информацию о пользователе
- Любое другое сообщение - покажет список доступных команд

## 📊 Формат вывода

Бот показывает информацию в формате JSON:
```json
{
  "id": 860125116,
  "first_name": "Шикльгрубер",
  "last_name": "",
  "username": "pshod",
  "language_code": "ru",
  "is_premium": true,
  "allows_write_to_pm": true,
  "photo_url": "..."
}
```

## 🔧 Настройка

Все настройки находятся в файле `.env`. Основной параметр:
- `BOT_TOKEN` - токен вашего бота от @BotFather

## 📝 Примечания

- Бот показывает только ту информацию, которую Telegram предоставляет через API
- `photo_url` может быть недоступен в некоторых случаях
- `is_premium` доступен только если пользователь имеет Telegram Premium
- `allows_write_to_pm` всегда `true`, если пользователь написал боту

