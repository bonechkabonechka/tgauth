# Инструкция по настройке и деплою на Vercel

## 📋 Что было сделано

Backend переписан с Firebase Functions на Vercel API Routes с использованием **Turso (libSQL)** - распределенного SQLite, который работает в serverless окружении.

## 🗂️ Структура проекта

```
ton10/
├── api/                    # Vercel API Routes
│   ├── auth/
│   │   ├── signin.ts      # POST /api/auth/signin
│   │   └── protected.ts   # GET /api/auth/protected
│   ├── db/
│   │   ├── init.ts        # Инициализация Turso БД
│   │   └── users.ts       # Работа с пользователями
│   └── utils/
│       ├── jwt.ts         # JWT токены
│       └── validateInitData.ts  # Валидация Telegram initData
├── src/                    # React frontend
├── vercel.json            # Конфигурация Vercel
└── package.json           # Зависимости
```

## 🚀 Шаг 1: Настройка Turso (SQLite база данных)

### 1.1. Создайте аккаунт на Turso

1. Перейдите на https://turso.tech/
2. Зарегистрируйтесь (можно через GitHub)
3. Создайте новый проект

### 1.2. Создайте базу данных

1. В панели Turso нажмите "Create Database"
2. Выберите регион (ближайший к вам)
3. Назовите базу (например: `ton10-db`)
4. После создания откройте базу и перейдите в "Connect"

### 1.3. Получите токены

1. В разделе "Connect" найдите:
   - **Database URL** (например: `libsql://your-db-name-xxx.turso.io`)
   - **Auth Token** (нажмите "Show" чтобы увидеть)

2. Сохраните эти значения - они понадобятся для переменных окружения

## 🔧 Шаг 2: Установка зависимостей

```bash
cd C:\ts\ton\ton9\ton10
npm install
```

Это установит:
- `@libsql/client` - клиент для Turso (SQLite)
- `@vercel/node` - типы для Vercel API
- `jsonwebtoken` - JWT токены
- `cookie` - парсинг cookies

## 🔐 Шаг 3: Настройка переменных окружения

### 3.1. Локальная разработка

Создайте файл `.env.local` в корне проекта (`ton10/.env.local`):

```env
# Turso Database
TURSO_DATABASE_URL=libsql://your-db-name-xxx.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here

# Telegram Bot
BOT_TOKEN=your-bot-token-from-botfather

# JWT Secrets (сгенерируйте случайные строки минимум 64 символа)
JWT_ACCESS_SECRET=your-super-secret-access-key-min-64-chars-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-64-chars-change-in-production
```

**⚠️ ВАЖНО:** 
- `.env.local` уже должен быть в `.gitignore` (не коммитьте его!)
- Используйте разные секреты для access и refresh токенов
- Сгенерируйте случайные строки (можно использовать: `openssl rand -hex 32`)

### 3.2. Vercel (продакшен)

1. Перейдите на https://vercel.com/
2. Войдите в аккаунт
3. Импортируйте проект (или создайте новый)
4. Перейдите в **Settings → Environment Variables**
5. Добавьте все переменные из `.env.local`:
   - `TURSO_DATABASE_URL`
   - `TURSO_AUTH_TOKEN`
   - `BOT_TOKEN`
   - `JWT_ACCESS_SECRET`
   - `JWT_REFRESH_SECRET`

## 🗄️ Шаг 4: Инициализация базы данных

База данных инициализируется автоматически при первом запросе к API. Таблица `users` создается автоматически.

Если хотите проверить вручную, можете использовать Turso CLI:

```bash
# Установите Turso CLI (опционально)
# Windows: scoop install turso
# Или скачайте с https://docs.turso.tech/cli/installation

# Подключитесь к базе
turso db shell your-db-name

# Проверьте таблицы
.tables

# Посмотрите структуру users
.schema users
```

## 🧪 Шаг 5: Локальная разработка

### 5.1. Запуск frontend

```bash
cd C:\ts\ton\ton9\ton10
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

### 5.2. Тестирование API локально

Vercel API routes работают через Vercel CLI:

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Запустите dev сервер
vercel dev
```

Это запустит и frontend, и API на `http://localhost:3000`

**Альтернатива:** Можно использовать Vite proxy для разработки (см. `vite.config.ts`)

## 🚀 Шаг 6: Деплой на Vercel

### 6.1. Через Vercel CLI

```bash
cd C:\ts\ton\ton9\ton10

# Войдите в Vercel (если еще не вошли)
vercel login

# Задеплойте проект
vercel

# Для продакшена
vercel --prod
```

### 6.2. Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub
2. Запушьте код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/ton10.git
   git push -u origin main
   ```
3. На https://vercel.com/ нажмите "Add New Project"
4. Импортируйте репозиторий
5. Vercel автоматически определит настройки из `vercel.json`
6. Добавьте переменные окружения (см. Шаг 3.2)
7. Нажмите "Deploy"

### 6.3. После деплоя

1. Vercel даст вам URL (например: `https://ton10.vercel.app`)
2. Обновите настройки Telegram Mini App:
   - Откройте @BotFather
   - `/mybots` → выберите бота → "Bot Settings" → "Menu Button"
   - Укажите URL: `https://ton10.vercel.app`

## 📝 Шаг 7: Проверка работы

### 7.1. Проверка API endpoints

```bash
# Проверка signin (нужен initData от Telegram)
curl -X POST https://your-app.vercel.app/api/auth/signin \
  -H "Content-Type: application/json" \
  -d '{"initData":"your-telegram-initdata"}'

# Проверка protected (нужны cookies)
curl -X GET https://your-app.vercel.app/api/auth/protected \
  -H "Cookie: ACCESS_TOKEN=...; REFRESH_TOKEN=..."
```

### 7.2. Проверка через Mini App

1. Откройте бота в Telegram
2. Нажмите на кнопку меню (если настроена)
3. Должна открыться Mini App
4. Проверьте авторизацию и отображение данных пользователя

## 🔍 Отладка

### Просмотр логов Vercel

```bash
# Через CLI
vercel logs

# Или в веб-интерфейсе
# Vercel Dashboard → Your Project → Functions → Logs
```

### Проверка базы данных

```bash
# Через Turso CLI
turso db shell your-db-name

# Посмотреть всех пользователей
SELECT * FROM users;
```

## ⚠️ Важные замечания

1. **Turso бесплатный tier:**
   - 500 MB хранилища
   - 1 миллион строк в месяц
   - Достаточно для тестирования и небольших проектов

2. **Vercel бесплатный tier:**
   - 100 GB bandwidth
   - Serverless Functions: 100 GB-hours
   - Достаточно для большинства проектов

3. **Безопасность:**
   - Никогда не коммитьте `.env.local`
   - Используйте сильные JWT секреты (64+ символов)
   - В продакшене `secure: true` для cookies (только HTTPS)

4. **CORS:**
   - Настроен для работы с Telegram Mini Apps
   - В продакшене можно ограничить origin

## 🆘 Решение проблем

### Ошибка: "TURSO_DATABASE_URL не установлен"
- Проверьте переменные окружения в Vercel Dashboard
- Убедитесь что они добавлены для нужного окружения (Production/Preview/Development)

### Ошибка: "Invalid initData signature"
- Проверьте `BOT_TOKEN` - должен совпадать с токеном бота
- Убедитесь что `initData` передается правильно

### Cookies не устанавливаются
- Проверьте что запрос идет по HTTPS (в продакшене)
- Проверьте настройки браузера (не блокирует ли cookies)
- В локальной разработке может не работать `Secure` флаг

## 📚 Полезные ссылки

- [Turso Documentation](https://docs.turso.tech/)
- [Vercel API Routes](https://vercel.com/docs/functions/serverless-functions)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [JWT Best Practices](https://datatracker.ietf.org/doc/html/rfc8725)

---

**Готово!** Ваш проект должен работать на Vercel с SQLite через Turso. 🎉

