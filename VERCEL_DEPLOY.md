# 🚀 Деплой на Vercel с GitHub

## 📋 Быстрая инструкция

Ваш проект уже на GitHub: https://github.com/bonechkabonechka/tgauth

### Шаг 1: Настройте Turso (SQLite база данных)

1. Зарегистрируйтесь на https://turso.tech/ (можно через GitHub)
2. Создайте новую базу данных:
   - Нажмите "Create Database"
   - Выберите регион
   - Назовите базу (например: `tgauth-db`)
3. Получите токены:
   - Откройте базу → "Connect"
   - Скопируйте **Database URL** (например: `libsql://tgauth-db-xxx.turso.io`)
   - Скопируйте **Auth Token** (нажмите "Show")

### Шаг 2: Подключите проект к Vercel

1. Откройте https://vercel.com/
2. Войдите (можно через GitHub)
3. Нажмите **"Add New Project"**
4. Импортируйте репозиторий:
   - Выберите `bonechkabonechka/tgauth`
   - Или введите: `https://github.com/bonechkabonechka/tgauth`
5. Настройте проект:
   - **Framework Preset:** Vite
   - **Root Directory:** `./` (оставьте как есть)
   - **Build Command:** `npm run build` (уже указано)
   - **Output Directory:** `dist` (уже указано)
6. **НЕ нажимайте Deploy пока!** Сначала настройте переменные окружения

### Шаг 3: Настройте переменные окружения в Vercel

1. В настройках проекта нажмите **"Environment Variables"**
2. Добавьте следующие переменные:

```
TURSO_DATABASE_URL = libsql://your-db-name-xxx.turso.io
TURSO_AUTH_TOKEN = your-auth-token-here
BOT_TOKEN = your-bot-token-from-botfather
JWT_ACCESS_SECRET = your-access-secret-min-64-chars
JWT_REFRESH_SECRET = your-refresh-secret-min-64-chars
```

**⚠️ ВАЖНО:**
- Замените все значения на реальные
- `JWT_ACCESS_SECRET` и `JWT_REFRESH_SECRET` должны быть разными случайными строками минимум 64 символа
- Можно сгенерировать: `openssl rand -hex 32` (повторите дважды)

3. Выберите окружения: **Production**, **Preview**, **Development** (или только Production)
4. Нажмите **"Save"**

### Шаг 4: Задеплойте проект

1. Вернитесь на вкладку **"Deploy"**
2. Нажмите **"Deploy"**
3. Дождитесь завершения сборки (обычно 1-2 минуты)

### Шаг 5: Проверьте работу

1. После деплоя Vercel даст вам URL (например: `https://tgauth.vercel.app`)
2. Откройте этот URL в браузере
3. Должна открыться ваша Mini App!

### Шаг 6: Настройте Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Укажите URL вашего Vercel проекта (например: `https://tgauth.vercel.app`)
6. Сохраните

---

## ✅ Готово!

Ваш проект должен работать на Vercel!

---

## 🔄 Обновление проекта

После каждого `git push` в репозиторий, Vercel автоматически:
1. Обнаружит изменения
2. Запустит новую сборку
3. Задеплоит обновленную версию

Вы можете отключить автоматический деплой в настройках проекта.

---

## 🧪 Локальная разработка

### Установите Vercel CLI

```bash
npm i -g vercel
```

### Запустите локальный сервер

```bash
cd C:\ts\ton\ton9\ton10

# Войдите в Vercel
vercel login

# Запустите dev сервер
vercel dev
```

Это запустит:
- Frontend на `http://localhost:3000`
- API routes на `http://localhost:3000/api`

### Настройте локальные переменные окружения

Создайте файл `.env.local`:

```env
TURSO_DATABASE_URL=libsql://your-db-name-xxx.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here
BOT_TOKEN=your-bot-token-from-botfather
JWT_ACCESS_SECRET=your-access-secret-min-64-chars
JWT_REFRESH_SECRET=your-refresh-secret-min-64-chars
```

---

## 🔍 Проверка работы

### Проверка API endpoints

```bash
# Health check (если добавите endpoint)
curl https://your-project.vercel.app/api/auth/protected

# Или через браузер откройте DevTools → Network
```

### Просмотр логов

1. В Vercel Dashboard откройте ваш проект
2. Перейдите в **"Functions"** → **"Logs"**
3. Там будут все логи из API routes

---

## 🆘 Решение проблем

### Ошибка: "TURSO_DATABASE_URL не установлен"

- Проверьте что переменные окружения добавлены в Vercel Dashboard
- Убедитесь что они добавлены для нужного окружения (Production/Preview)

### Ошибка: "Invalid initData signature"

- Проверьте `BOT_TOKEN` - должен совпадать с токеном бота
- Убедитесь что `initData` передается правильно

### Cookies не устанавливаются

- В продакшене на Vercel cookies должны работать автоматически
- Проверьте что запрос идет по HTTPS
- Проверьте настройки браузера

### База данных не инициализируется

- База инициализируется автоматически при первом запросе к `/api/auth/signin`
- Проверьте логи в Vercel Dashboard → Functions → Logs

---

## 📚 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel API Routes](https://vercel.com/docs/functions/serverless-functions)
- [Turso Documentation](https://docs.turso.tech/)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)

---

**Готово!** Ваш проект должен работать на Vercel! 🎉

