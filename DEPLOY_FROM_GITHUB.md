# 🚀 Деплой с GitHub на сервер 194.87.102.27

## 📋 Пошаговая инструкция

### Шаг 1: Подключитесь к серверу

```bash
ssh root@194.87.102.27
```

Если у вас другой пользователь или ключ SSH:
```bash
ssh your-user@194.87.102.27
# или
ssh -i /path/to/your/key.pem root@194.87.102.27
```

---

### Шаг 2: Установите Docker (если еще не установлен)

Выполните эти команды на сервере:

```bash
# Обновите систему
apt update && apt upgrade -y

# Установите необходимые пакеты
apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# Добавьте официальный GPG ключ Docker
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Добавьте репозиторий Docker
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установите Docker
apt update
apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Проверьте установку
docker --version
docker compose version
```

**Ожидаемый результат:**
```
Docker version 24.x.x
Docker Compose version v2.x.x
```

---

### Шаг 3: Клонируйте проект с GitHub

```bash
# Перейдите в директорию /opt
cd /opt

# Клонируйте репозиторий
git clone https://github.com/bonechkabonechka/tgauth.git

# Перейдите в директорию проекта
cd tgauth
```

---

### Шаг 4: Создайте файл с переменными окружения

```bash
# Создайте файл .env
nano .env
```

Добавьте следующее содержимое (замените значения на реальные):

```env
NODE_ENV=production
PORT=3000

# Telegram Bot Token (получите от @BotFather)
BOT_TOKEN=your-bot-token-from-botfather

# JWT Secrets (сгенерируйте случайные строки минимум 64 символа)
# Можно использовать: openssl rand -hex 32 (повторите дважды)
JWT_ACCESS_SECRET=your-super-secret-access-key-min-64-chars-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-64-chars-change-this

# Database Path
DB_PATH=/app/data/database.db

# CORS (можно оставить * или указать конкретный домен)
ALLOWED_ORIGIN=*
```

**Как сохранить в nano:**
- Нажмите `Ctrl+O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl+X` (выйти)

**⚠️ ВАЖНО:** 
- Замените `your-bot-token-from-botfather` на реальный токен бота
- Сгенерируйте уникальные JWT секреты (минимум 64 символа каждый)

---

### Шаг 5: Создайте необходимые директории

```bash
# Создайте директории для БД, логов и SSL
mkdir -p data logs nginx/ssl

# Установите правильные права доступа
chmod 755 data logs
```

---

### Шаг 6: Соберите Docker образы

```bash
# Убедитесь что вы в директории проекта
cd /opt/tgauth

# Соберите образы (это может занять несколько минут)
docker compose build
```

**Ожидаемый результат:**
```
[+] Building ...
[+] Building 123.4s (15/15) FINISHED
...
Successfully built ...
```

---

### Шаг 7: Запустите контейнеры

```bash
# Запустите контейнеры в фоновом режиме
docker compose up -d
```

**Ожидаемый результат:**
```
[+] Running 3/3
 ✔ Network tgauth_ton10-network    Created
 ✔ Container tgauth-app-1          Started
 ✔ Container tgauth-nginx-1         Started
```

---

### Шаг 8: Проверьте статус

```bash
# Проверьте что контейнеры запущены
docker compose ps
```

**Ожидаемый результат:**
```
NAME              STATUS          PORTS
tgauth-app-1      Up 2 minutes    0.0.0.0:3000->3000/tcp
tgauth-nginx-1    Up 2 minutes    0.0.0.0:80->80/tcp, 0.0.0.0:443->443/tcp
```

---

### Шаг 9: Проверьте логи

```bash
# Просмотрите логи приложения
docker compose logs app

# Или следите за логами в реальном времени
docker compose logs -f app
```

**Что искать в логах:**
```
🚀 Server running on port 3000
📁 Environment: production
🌐 API: http://localhost:3000/api
```

Если видите ошибки - проверьте `.env` файл и убедитесь что все переменные заполнены правильно.

---

### Шаг 10: Проверьте работу приложения

#### 10.1. Проверьте health endpoint

```bash
curl http://localhost:3000/health
```

**Ожидаемый результат:**
```json
{"status":"ok","timestamp":"2025-01-07T..."}
```

#### 10.2. Откройте в браузере

Откройте в браузере: **http://194.87.102.27**

Должна открыться ваша Telegram Mini App!

---

### Шаг 11: Настройте Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Укажите URL: `http://194.87.102.27`
6. Сохраните

---

## ✅ Готово!

Ваш проект должен работать на **http://194.87.102.27**

---

## 🔄 Полезные команды для управления

### Просмотр логов
```bash
# Все логи
docker compose logs -f

# Только приложение
docker compose logs -f app

# Только Nginx
docker compose logs -f nginx
```

### Управление контейнерами
```bash
# Остановить
docker compose down

# Запустить
docker compose up -d

# Перезапустить
docker compose restart

# Перезапустить только приложение
docker compose restart app
```

### Обновление проекта
```bash
cd /opt/tgauth

# Получите последние изменения
git pull

# Пересоберите образы
docker compose build

# Перезапустите контейнеры
docker compose up -d
```

### Проверка статуса
```bash
# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Проверка здоровья
curl http://localhost:3000/health
```

---

## 🆘 Решение проблем

### Проблема: Контейнер не запускается

```bash
# Проверьте логи
docker compose logs app

# Проверьте конфигурацию
docker compose config

# Проверьте .env файл
cat .env
```

### Проблема: Порт 3000 занят

```bash
# Проверьте что использует порт
lsof -i :3000

# Или измените порт в docker-compose.yml
nano docker-compose.yml
# Измените "3000:3000" на "3001:3000"
```

### Проблема: База данных не создается

```bash
# Проверьте права доступа
ls -la /opt/tgauth/data/

# Проверьте что директория смонтирована
docker compose exec app ls -la /app/data
```

### Проблема: Nginx не проксирует запросы

```bash
# Проверьте логи Nginx
docker compose logs nginx

# Проверьте конфигурацию
docker compose exec nginx nginx -t

# Перезапустите Nginx
docker compose restart nginx
```

### Проблема: Приложение не отвечает

```bash
# Проверьте что контейнер запущен
docker compose ps

# Проверьте health endpoint
curl http://localhost:3000/health

# Перезапустите
docker compose restart app
```

---

## 🔒 Настройка SSL (HTTPS) - опционально

Если хотите использовать HTTPS:

### Вариант 1: С доменом

Если у вас есть домен, указывающий на 194.87.102.27:

```bash
# Установите certbot в контейнере
docker compose exec nginx sh -c "apk add --no-cache certbot certbot-nginx"

# Получите сертификат
docker compose exec nginx certbot --nginx -d your-domain.com
```

### Вариант 2: Самоподписанный сертификат

```bash
# Создайте самоподписанный сертификат
docker compose exec nginx sh -c "openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /etc/nginx/ssl/privkey.pem -out /etc/nginx/ssl/fullchain.pem -subj '/CN=194.87.102.27'"

# Раскомментируйте HTTPS секцию в nginx/conf.d/default.conf
nano nginx/conf.d/default.conf

# Перезапустите Nginx
docker compose restart nginx
```

---

## 📊 Мониторинг

### Просмотр использования ресурсов
```bash
docker stats
```

### Просмотр логов в реальном времени
```bash
docker compose logs -f app
```

---

## 🗄️ Резервное копирование БД

### Создайте скрипт бэкапа

```bash
nano /root/backup-tgauth.sh
```

Добавьте:

```bash
#!/bin/bash
BACKUP_DIR="/root/backups/tgauth"
DB_PATH="/opt/tgauth/data/database.db"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR
cp $DB_PATH "$BACKUP_DIR/database_$DATE.db"

# Удалите старые бэкапы (старше 7 дней)
find $BACKUP_DIR -name "database_*.db" -mtime +7 -delete

echo "Backup created: database_$DATE.db"
```

Сделайте исполняемым:

```bash
chmod +x /root/backup-tgauth.sh
```

### Настройте автоматический бэкап

```bash
crontab -e
```

Добавьте (бэкап каждый день в 3:00):

```
0 3 * * * /root/backup-tgauth.sh
```

---

## 📝 Чеклист деплоя

- [ ] Подключились к серверу
- [ ] Установили Docker
- [ ] Клонировали проект с GitHub
- [ ] Создали `.env` файл с правильными значениями
- [ ] Создали директории `data`, `logs`, `nginx/ssl`
- [ ] Собрали Docker образы (`docker compose build`)
- [ ] Запустили контейнеры (`docker compose up -d`)
- [ ] Проверили статус (`docker compose ps`)
- [ ] Проверили логи (`docker compose logs app`)
- [ ] Проверили health endpoint (`curl http://localhost:3000/health`)
- [ ] Открыли сайт в браузере (`http://194.87.102.27`)
- [ ] Настроили Telegram Mini App URL
- [ ] Настроили бэкапы (опционально)

---

**Готово!** Ваш проект должен работать на **http://194.87.102.27** 🎉

Если возникнут проблемы - проверьте логи: `docker compose logs -f app`

