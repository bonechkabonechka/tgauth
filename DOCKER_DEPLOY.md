# Инструкция по деплою на сервер через Docker

## 🎯 Ваш сервер: 194.87.102.27

## 📋 Что нужно сделать

### Шаг 1: Подготовка сервера

#### 1.1. Подключитесь к серверу

```bash
ssh root@194.87.102.27
# Или если у вас другой пользователь:
# ssh your-user@194.87.102.27
```

#### 1.2. Обновите систему

```bash
apt update && apt upgrade -y
```

#### 1.3. Установите Docker и Docker Compose

```bash
# Установите необходимые пакеты
apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# Добавьте официальный GPG ключ Docker
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Добавьте репозиторий Docker
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установите Docker
apt update
apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Проверьте установку
docker --version
docker compose version
```

#### 1.4. Настройте firewall (опционально, но рекомендуется)

```bash
# Установите UFW если еще не установлен
apt install -y ufw

# Разрешите SSH, HTTP и HTTPS
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

---

### Шаг 2: Подготовка проекта на локальной машине

#### 2.1. Создайте файл `.env` на сервере

На вашем компьютере создайте файл `.env.production`:

```env
NODE_ENV=production
PORT=3000

# Telegram Bot Token
BOT_TOKEN=your-bot-token-from-botfather

# JWT Secrets (сгенерируйте случайные строки минимум 64 символа)
JWT_ACCESS_SECRET=your-super-secret-access-key-min-64-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-64-chars

# Database Path
DB_PATH=/app/data/database.db

# CORS (можно оставить * или указать конкретный домен)
ALLOWED_ORIGIN=*
```

**⚠️ ВАЖНО:** Замените значения на реальные!

#### 2.2. Загрузите проект на сервер

**Вариант A: Через Git (рекомендуется)**

```bash
# На сервере
cd /opt
git clone https://github.com/your-username/ton10.git
cd ton10
```

**Вариант B: Через SCP**

```bash
# На вашем компьютере (из директории проекта)
cd C:\ts\ton\ton9\ton10

# Создайте архив (исключая ненужные файлы)
tar -czf ton10.tar.gz --exclude='node_modules' --exclude='dist' --exclude='dist-server' --exclude='data' --exclude='.git' .

# Загрузите на сервер
scp ton10.tar.gz root@194.87.102.27:/opt/

# На сервере распакуйте
ssh root@194.87.102.27
cd /opt
mkdir ton10
cd ton10
tar -xzf ../ton10.tar.gz
```

---

### Шаг 3: Настройка на сервере

#### 3.1. Создайте директории

```bash
cd /opt/ton10
mkdir -p data logs nginx/ssl
chmod 755 data logs
```

#### 3.2. Создайте файл `.env`

```bash
nano .env
```

Скопируйте содержимое из `.env.production` и сохраните (`Ctrl+O`, `Enter`, `Ctrl+X`)

#### 3.3. Проверьте конфигурацию

```bash
# Проверьте что все файлы на месте
ls -la
ls -la nginx/
ls -la server/
```

---

### Шаг 4: Сборка и запуск Docker контейнеров

#### 4.1. Соберите образ

```bash
cd /opt/ton10
docker compose build
```

Это может занять несколько минут при первом запуске.

#### 4.2. Запустите контейнеры

```bash
docker compose up -d
```

Флаг `-d` запускает контейнеры в фоновом режиме.

#### 4.3. Проверьте статус

```bash
docker compose ps
```

Должны быть запущены два контейнера:
- `ton10-app` (ваше приложение)
- `ton10-nginx` (Nginx reverse proxy)

#### 4.4. Проверьте логи

```bash
# Логи приложения
docker compose logs app

# Логи Nginx
docker compose logs nginx

# Все логи
docker compose logs -f
```

---

### Шаг 5: Проверка работы

#### 5.1. Проверьте health endpoint

```bash
curl http://localhost:3000/health
# Должно вернуть: {"status":"ok","timestamp":"..."}
```

#### 5.2. Проверьте через браузер

Откройте в браузере: `http://194.87.102.27`

Должна открыться ваша Mini App!

---

### Шаг 6: Настройка SSL (HTTPS) - опционально, но рекомендуется

#### 6.1. Установите Certbot в контейнере Nginx

```bash
# Войдите в контейнер Nginx
docker compose exec nginx sh

# В контейнере установите certbot
apk add --no-cache certbot certbot-nginx

# Выйдите из контейнера
exit
```

#### 6.2. Получите SSL сертификат

**Если у вас есть домен, указывающий на 194.87.102.27:**

```bash
# Запустите certbot в контейнере
docker compose exec nginx certbot --nginx -d your-domain.com

# Следуйте инструкциям
```

**Если домена нет, можно использовать самоподписанный сертификат:**

```bash
# Создайте самоподписанный сертификат
docker compose exec nginx sh -c "openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /etc/nginx/ssl/privkey.pem -out /etc/nginx/ssl/fullchain.pem -subj '/CN=194.87.102.27'"

# Обновите nginx/conf.d/default.conf - раскомментируйте HTTPS секцию
nano nginx/conf.d/default.conf

# Перезапустите Nginx
docker compose restart nginx
```

---

## 🔄 Управление контейнерами

### Полезные команды

```bash
# Остановить контейнеры
docker compose down

# Запустить контейнеры
docker compose up -d

# Перезапустить контейнеры
docker compose restart

# Перезапустить только приложение
docker compose restart app

# Перезапустить только Nginx
docker compose restart nginx

# Просмотр логов
docker compose logs -f app
docker compose logs -f nginx

# Просмотр статуса
docker compose ps

# Войти в контейнер приложения
docker compose exec app sh

# Войти в контейнер Nginx
docker compose exec nginx sh

# Пересобрать образы
docker compose build --no-cache

# Остановить и удалить все (БД сохранится в ./data)
docker compose down
```

---

## 🔄 Обновление приложения

### Когда нужно обновить код:

```bash
cd /opt/ton10

# Если используете Git:
git pull

# Пересоберите образы
docker compose build

# Перезапустите контейнеры
docker compose up -d

# Проверьте логи
docker compose logs -f app
```

---

## 📊 Мониторинг

### Просмотр использования ресурсов

```bash
docker stats
```

### Просмотр логов

```bash
# Все логи
docker compose logs -f

# Только ошибки приложения
docker compose logs app | grep -i error

# Последние 100 строк
docker compose logs --tail=100 app
```

---

## 🗄️ Резервное копирование БД

### Создайте скрипт бэкапа

```bash
nano /root/backup-ton10.sh
```

Добавьте:

```bash
#!/bin/bash
BACKUP_DIR="/root/backups/ton10"
DB_PATH="/opt/ton10/data/database.db"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR
cp $DB_PATH "$BACKUP_DIR/database_$DATE.db"

# Удалите старые бэкапы (старше 7 дней)
find $BACKUP_DIR -name "database_*.db" -mtime +7 -delete

echo "Backup created: database_$DATE.db"
```

Сделайте исполняемым:

```bash
chmod +x /root/backup-ton10.sh
```

### Настройте автоматический бэкап

```bash
crontab -e
```

Добавьте (бэкап каждый день в 3:00):

```
0 3 * * * /root/backup-ton10.sh
```

---

## 🔧 Решение проблем

### Контейнер не запускается

```bash
# Проверьте логи
docker compose logs app

# Проверьте конфигурацию
docker compose config
```

### Порт 3000 занят

```bash
# Проверьте что использует порт
lsof -i :3000

# Или измените порт в docker-compose.yml
```

### Nginx не проксирует запросы

```bash
# Проверьте логи Nginx
docker compose logs nginx

# Проверьте конфигурацию
docker compose exec nginx nginx -t
```

### База данных не создается

```bash
# Проверьте права доступа
ls -la /opt/ton10/data/

# Проверьте что директория смонтирована
docker compose exec app ls -la /app/data
```

### Приложение не отвечает

```bash
# Проверьте health endpoint
curl http://localhost:3000/health

# Проверьте что контейнер запущен
docker compose ps

# Перезапустите
docker compose restart app
```

---

## 📝 Настройка Telegram Mini App

После успешного деплоя:

1. Откройте @BotFather в Telegram
2. `/mybots` → выберите вашего бота
3. "Bot Settings" → "Menu Button"
4. Укажите URL: `http://194.87.102.27` (или `https://your-domain.com` если настроили SSL)

---

## ✅ Чеклист деплоя

- [ ] Docker и Docker Compose установлены
- [ ] Проект загружен на сервер
- [ ] Файл `.env` создан и заполнен
- [ ] Директории `data` и `logs` созданы
- [ ] Контейнеры собраны (`docker compose build`)
- [ ] Контейнеры запущены (`docker compose up -d`)
- [ ] Health endpoint отвечает (`curl http://localhost:3000/health`)
- [ ] Сайт открывается в браузере (`http://194.87.102.27`)
- [ ] SSL настроен (опционально)
- [ ] Telegram Mini App URL обновлен
- [ ] Бэкапы настроены

---

**Готово!** Ваш проект должен работать на сервере 194.87.102.27! 🎉

Если возникнут проблемы - проверьте логи: `docker compose logs -f`

